name: Keep Supabase Awake

on:
  schedule:
    # Runs every 3 days at midnight UTC
    - cron: '0 0 */3 * *'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  ping-database:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -s -o /dev/null -w "%{http_code}" "https://psrkbwpxkeljyalvitla.supabase.co/rest/v1/products?select=id&limit=1" \
          -H "apikey: sb_publishable_tSI-FQlMWlM_HTP0CJdEmg_VXqzzLus" \
          -H "Authorization: Bearer sb_publishable_tSI-FQlMWlM_HTP0CJdEmg_VXqzzLus"
